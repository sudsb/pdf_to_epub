# PToEA 矫正界面帮助文档

## 目录
1. [Markdown 格式语法](#markdown-格式语法)
2. [选中文字后的快捷菜单](#选中文字后的快捷菜单)
3. [快捷键绑定](#快捷键绑定)
4. [格式规则引擎](#格式规则引擎)
5. [正则表达式使用指南](#正则表达式使用指南)
6. [其他功能](#其他功能)
7. [常见问题](#常见问题)

---

## Markdown 格式语法

在「Markdown 模式」下，可直接输入以下语法：

| 语法 | 效果 | 示例 |
|------|------|------|
| `# 标题` | 一级标题 | `# 章节标题` |
| `## 标题` | 二级标题 | `## 小节标题` |
| `### 标题` | 三级标题 | `### 小小节标题` |
| `**文字**` | 粗体 | `**重要内容**` |
| `__文字__` | 粗体 | `__重要内容__` |
| `*文字*` | 斜体 | `*强调内容*` |
| `_文字_` | 斜体 | `_强调内容_` |
| `` `代码` `` | 行内代码 | `` `print("hello")` `` |
| `[文字](网址)` | 链接 | `[百度](https://baidu.com)` |
| `- 项目` | 无序列表 | `- 第一项\n- 第二项` |
| `1. 项目` | 有序列表 | `1. 第一步\n2. 第二步` |
| `> 引用` | 引用块 | `> 这是引用内容` |
| ```` ```代码块``` ```` | 代码块 | 见下方 |

**代码块示例：**
````markdown
```python
def hello():
    print("Hello, World!")
```
````

> **注意**：Markdown 模式下直接输入上述语法即可；「全文/段落/注释标记」以行内 HTML（`<span data-ptoe-marker=…>`）形式插入并原样保留。点击工具栏「Markdown模式」在源码与富文本之间切换。

---

## 选中文字后的快捷菜单

选中文字后会弹出快捷菜单（图标按钮），支持以下操作：

| 图标/按钮 | 功能 | 说明 |
|----------|------|------|
| **B** | 粗体 | 对选中块应用加粗 |
| **I** | 斜体 | 对选中块应用斜体 |
| **标** | 标题循环 | 循环切换：H1 → H2 → H3 → H4 → H5 → H6 → 正文 |
| **正** | 正文 | 将选中块转为普通段落 (p) |
| **清** | 清除格式 | 移除选中块的所有格式（加粗、斜体、对齐、标题等） |
| **注** | 注释格式 | 整段转为小字注释 (class="ptoe-note") |
| **左** | 居左对齐 | 设置选中块左对齐 |
| **中** | 居中对齐 | 设置选中块居中对齐 |
| **右** | 居右对齐 | 设置选中块右对齐 |
| **篇** | 全文标记 | 标记当前文章到此结束，后续内容属于新文章（生成 EPUB 时开新页） |
| **释** | 注释标记 | 插入到光标处，由对应注释段落替换；数量需一一匹配；自动加中文括号，已带括号的注释仅改字号 |
| **段** | 段落标记 | 段首=与上一段合并，段尾=与下一段合并 |
| **页** | 换页标记 | 从此处之后的内容显示在新的一页 |
| **引** | 引用格式 | 斜体 + 独立字体设置（需在设置中启用） |
| **不加粗** | 移除加粗 | 仅移除加粗，保留其他格式 |

---

## 快捷键绑定

### 默认快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+B` | 粗体 |
| `Ctrl+I` | 斜体 |
| `Ctrl+1` | 标题（循环 H1→H6→正文） |
| `Ctrl+0` | 正文 |
| `Ctrl+Shift+N` | 注释格式 |
| `Ctrl+Shift+←` | 居左对齐 |
| `Ctrl+Shift+↑` | 居中对齐 |
| `Ctrl+Shift+→` | 居右对齐 |
| `Ctrl+Shift+F` | 全文标记 |
| `Ctrl+Shift+M` | 注释标记 |
| `Ctrl+Shift+J` | 段落标记 |
| `Ctrl+Shift+P` | 换页标记 |
| `Ctrl+F` | 搜索/替换 |
| `Ctrl+Shift+C` | 智能清理 |
| `Ctrl+Shift+T` | 繁体转简体 |
| `Ctrl+Shift+Y` | 简体转繁体 |
| `Ctrl+Shift+D` | 切换 Markdown 模式 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` / `Ctrl+Shift+Z` | 重做 |
| `Ctrl+H` | 历史记录 |
| `Ctrl+E` | 导出 |
| `Ctrl+S` | 保存 |
| `Ctrl+Shift+S` | 暂存 |
| `Ctrl+Enter` | 完成并转换 |
| `Ctrl+G` | 跳转到页码 |
| `F1` | 帮助 |
| `Ctrl+Shift+O` | 设置 |
| `Ctrl+K` | 校正（纠错） |
| `Ctrl+Shift+R` | 重识别 |
| `Ctrl+Shift+A` | 应用纠错建议 |
| `Ctrl+Shift+X` | 清除纠错标注 |
| `Ctrl+Shift+Z` | 回退纠错 |

### 纠错悬浮窗快捷键（显示纠错建议时）

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 采纳建议（替换为第一个候选词） |
| `Esc` | 忽略建议（标记为已忽略） |

### 自定义快捷键

1. 点击工具栏「设置」按钮（或 `Ctrl+Shift+O`）
2. 点击「快捷键」标签页
3. 点击某行操作后，按下新的组合键完成绑定
4. `Del` / `Backspace` 清除绑定，`Esc` 取消
5. 绑定保存在本地浏览器（localStorage）并同步到配置文件

> **规则**：组合键必须包含修饰键（Ctrl/Alt/Shift）或功能键（F1-F12），`Enter` 和 `Escape` 可作为裸键使用。

---

## 格式规则引擎

### 概念

格式规则允许你定义「条件 → 格式」的自动化映射，对选中文字或整页批量应用格式。

### 规则结构

每条规则包含：
- **名称**：规则标识
- **求值模式**：
  - **第一个匹配即停**：按顺序判断条件，首个匹配的条件生效即停止
  - **所有匹配都应用**：所有匹配的条件格式按序叠加（冲突格式自动跳过）
- **条件列表**：按顺序判断的多个条件

### 条件结构

每个条件包含：
- **类型**：
  - `正则匹配`：使用正则表达式匹配
  - `包含文字`：文本包含指定字符串
  - `以…开头`：文本以指定字符串开头
  - `以…结尾`：文本以指定字符串结尾
- **模式**：匹配的文本/正则
- **作用域**：
  - `选中`：仅在选中文字范围内匹配
  - `整段`：在光标所在段落内匹配
  - `当前页面`：在整页文本中匹配
- **匹配对象**（target）：
  - `匹配对象`：格式应用于匹配到的文本本身
  - `条件之前`：格式应用于匹配位置之前的文本
  - `条件之后`：格式应用于匹配位置之后的文本
  - `两条件之间`：格式应用于两个正则匹配之间的文本（需设置结束模式）
- **格式列表**：匹配时应用的格式操作（可多选，含「无」= 不处理文本）

### 正则条件的高级功能

#### 捕获组独立格式 (group_formats)
正则条件可为每个捕获组单独设置格式：
- 正则：`(标题)(内容)`
- 组1 格式：`标题1` + `加粗`
- 组2 格式：`正文`

#### 多次匹配独立格式 (match_formats)
同一正则全局匹配多次时，每次匹配可应用不同格式：
- 第1次匹配：`标题1`
- 第2次匹配：`标题2`
- 第3次匹配：`正文`

### 规则执行顺序

1. 规则按列表顺序从上到下执行
2. 同一规则内，条件按列表顺序判断
3. 冲突处理：
   - 块标签互斥：p/h1-h6 同一块只能有一个
   - 对齐互斥：左/中/右 同一块只能有一个
   - `remove` 与任何其他格式冲突（会清除全部格式）
   - bold/italic/citation/note 相互独立可共存
   - 冲突时**首个生效，后续跳过**

### 保存时冲突预警

保存规则时，若两条规则存在相同条件（type+pattern+scope）且格式互斥，会弹窗提示确认。

---

## 正则表达式使用指南

### 基础语法

| 语法 | 说明 |
|------|------|
| `.` | 匹配任意单字符（除换行符） |
| `\d` | 数字 `[0-9]` |
| `\D` | 非数字 |
| `\s` | 空白字符（空格、制表、换行） |
| `\S` | 非空白字符 |
| `\w` | 单词字符（字母、数字、下划线、汉字） |
| `\W` | 非单词字符 |
| `^` | 行首 |
| `$` | 行尾 |
| `[abc]` | 字符集合（a 或 b 或 c） |
| `[^abc]` | 排除字符集合 |
| `(...)` | 捕获组（可用 `$1` `$2` 反向引用） |
| `(?:...)` | 非捕获组（不占用组编号） |
| `a\|b` | 或（a 或 b） |

### 量词

| 语法 | 说明 |
|------|------|
| `*` | 0 次或多次（贪婪） |
| `+` | 1 次或多次（贪婪） |
| `?` | 0 次或 1 次 |
| `{n}` | 正好 n 次 |
| `{n,}` | 至少 n 次 |
| `{n,m}` | n 到 m 次 |
| `*?` `+?` `??` | 惰性匹配（尽可能少） |

### 常用模式示例

| 需求 | 正则 | 说明 |
|------|------|------|
| 中文数字日期 | `[(（]\s*[零〇一二三四五六七八九十]+\s*年\s*[零〇一二三四五六七八九十]+\s*月\s*[零〇一二三四五六七八九十]+\s*日\s*[）)]` | 匹配「（一九四九年十月十六日）」 |
| 可选"日" | `[(（]\s*.+\s*年\s*.+\s*月\s*(?:.+\s*日)?\s*[）)]` | 内层用 `(?:...)` 非捕获组 |
| 毛泽东落款 | `毛泽东[\s\S]*?刊印` | 惰性匹配到"刊印" |
| 注释标题 | `注\s*释` | 匹配"注 释"、"注释" |
| 连续标点 | `[，。！？；：、,.!?;:]{2,}` | 2个以上连续标点 |

### 标志位（/pattern/flags 语法）

支持 `/pattern/flags` 语法，如 `/正则/gi`：
- `g`：全局匹配（find all）
- `i`：忽略大小写
- `m`：多行模式（^$ 匹配行首尾）
- `s`：单行模式（. 匹配换行）

### 捕获组编号注意事项

**重要**：正则中的捕获组编号决定格式映射顺序。

```regex
# 错误：内层可选组会导致编号偏移
([\(（]\s*.+\s*年\s*.+\s*月\s*(.+\s*日)?\s*[\）)])

# 正确：内层用非捕获组 (?:...)
([\(（]\s*.+\s*年\s*.+\s*月\s*(?:.+\s*日)?\s*[\）)])
```

| 情况 | 外层组 | 内层组 | 后续组编号 |
|------|--------|--------|-----------|
| 无"日" | Group 1 | 无 | Group 2→Group 2 |
| 有"日" | Group 1 | Group 2 | Group 3→Group 3 |

**解决**：内层可选部分**必须**用 `(?:...)` 非捕获组。

---

## 格式规则实战案例

### 案例 1：自动识别日期行并设为标题

**规则设置：**
- 名称：`日期标题化`
- 模式：`第一个匹配即停`
- 条件 1：
  - 类型：`正则匹配`
  - 模式：`^[(（]\s*.+\s*年\s*.+\s*月\s*(?:.+\s*日)?\s*[）)]$`
  - 作用域：`整段`
  - 格式：`标题1` + `居中`

**效果**：形如「（一九四九年十月十六日）」的单独段落自动变为一级标题并居中。

---

### 案例 2：毛泽东落款格式化

**规则设置：**
- 名称：`落款格式`
- 模式：`第一个匹配即停`
- 条件 1：
  - 类型：`正则匹配`
  - 模式：`毛泽东[\s\S]*?刊印`
  - 作用域：`当前页面`
  - 匹配对象：`匹配对象`
  - 格式：`加粗` + `居右`

**效果**：从"毛泽东"到"刊印"的文本自动加粗靠右。

---

### 案例 3：注释区域自动格式化

**规则设置：**
- 名称：`注释区格式`
- 模式：`所有匹配都应用`
- 条件 1：
  - 类型：`正则匹配`
  - 模式：`注\s*释`
  - 作用域：`当前页面`
  - 匹配对象：`条件之后`
  - 格式：`注释`
- 条件 2：
  - 类型：`正则匹配`
  - 模式：`〔\d+〕`
  - 作用域：`当前页面`
  - 格式：`上标`（需配合字体设置）

**效果**："注 释"之后的所有内容自动应用注释格式（小字）。

---

### 案例 4：引用文献格式化

**规则设置：**
- 名称：`引用文献`
- 模式：`第一个匹配即停`
- 条件 1：
  - 类型：`正则匹配`
  - 模式：`〔\d+〕[\s\S]*?(?=〔\d+〕|$)`
  - 作用域：`当前页面`
  - 格式：`引用`

**效果**：每个脚注编号 `〔1〕` 到下一个编号或文末的内容应用引用格式（斜体+独立字体）。

---

## 其他功能

### 智能清理
工具栏「清理」按钮（或 `Ctrl+Shift+C`）：
- 合并相邻普通段落（前段不以句末标点结尾时）
- ASCII 字母数字相接处补空格
- 段首 `#/*` 等符号清理
- 加粗符号 `**...**` 清理
- 半角标点→全角 / 全角字母数字间标点→半角
- 保留已设标题/格式/标记/图片的段落结构

### 搜索替换
工具栏「搜」按钮（或 `Ctrl+F`）：
- 支持正则开关
- 结果列表显示 ±40 字上下文 + 页码 + 高亮
- 上限 200 条，点击跳转
- 「替换当前」仅替换选中那处，「全部替换」仅替换标签间文本 token

### 繁简转换
- `Ctrl+Shift+T`：繁体 → 简体
- `Ctrl+Shift+Y`：简体 → 繁体
- 转换后需保存生效

### 图片插入
- 左栏「图」按钮：插入当前预览图（全画幅/局部/行内）
- 左栏「裁」按钮：裁剪当前预览图后插入
- 工具栏「外部」按钮：选择本地图片插入
- 图片以 base64 data URL 内嵌，导出 EPUB 时自动提取

### 编辑器内图片拖拽
- 直接拖拽编辑区内的图片移动位置
- 自动保持包裹 `<p class="ptoe-img-full/fit">` 结构

### 历史记录
- `Ctrl+H` 打开历史面板
- 同一文件保留最近 20 个版本
- 支持多选删除、导入导出、版本加载
- 版本包含：页面文本、纠错标注、最后校正页码

### 纠错（校对）
工具栏「校」下拉菜单：
- **校正**：对当前页运行规则纠错（连续重复/连续标点/中英混排/词典滑窗）
- **应用**：当前页全部有候选条目替换为第一候选
- **清除**：清除当前页纠错标注（已应用文字保留）
- **回退**：按原始快照彻底恢复当前页纠错前文本

### 大模型重识别
右键菜单「重识别」：调用大模型重新识别当前页图片，生成差异标注供采纳。

### 导出
工具栏「导出」：
- **TXT**：UTF-8-BOM + CRLF，兼容 Windows 记事本
- **DOCX**：纯 stdlib 实现，标题带大纲层级
- **EPUB**：调用完整转换流程

---

## 常见问题

### Q: 正则匹配不到内容？
**A**: 检查以下几点：
1. 作用域是否正确（选中/整段/当前页面）
2. 正则中是否有未转义的特殊字符（如 `( ) [ ] . * + ?`）
3. 文本中是否有不可见字符（零宽空格、特殊空白）
4. 使用 `/pattern/s` 单行模式让 `.` 匹配换行
5. 在浏览器控制台测试：`text.match(/你的正则/)`

### Q: 捕获组格式没生效？
**A**: 
1. 确认正则捕获组数量与 `group_formats` 数组长度一致
2. 内层可选组必须用 `(?:...)` 非捕获组
3. 检查格式规则编辑器中各组是否勾选了正确格式

### Q: 规则保存后重新打开格式丢失？
**A**: 
1. 确认 `config.json` 中 `format_rules` 字段已正确写入
2. 检查 `_validate_format_rules` 是否过滤掉了非法格式
3. 旧模型规则会自动迁移，但 `else_formats` 会丢弃

### Q: 字体设置不生效？
**A**: 
1. 字体名称需系统已安装或使用 Web 安全字体族（serif/sans-serif/monospace/cursive/fantasy）
2. 多字体用逗号分隔，含空格的字体名加引号：`'Microsoft YaHei', sans-serif`
3. 修改后实时生效，但需点击「关闭」保存到配置文件

### Q: 撤销/重做不工作？
**A**: 
- 连续输入（间隔 <800ms）合并为一步
- 格式/标记/对齐/搜索替换/清理/繁简/插入图片各算一步
- 最多保留 10 步，新操作清空重做栈
- Markdown/富文本模式切换、历史版本载入会清空撤销栈

### Q: 导出 EPUB 目录不显示？
**A**: 
- 确认 `nav.xhtml` 包含 `epub:type="toc"` 和 `xmlns:epub` 命名空间
- 严格阅读器（Apple Books/Google Play）要求此声明
- 目录链接 `href="content_N.xhtml#h1"` 需与正文 `id="h1"` 对应

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-08 | 初始版本 |

---

*文档维护：PToEA 项目组 | 如有建议请反馈至项目仓库*