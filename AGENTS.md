# Repository Guidelines

---

## Project Overview

**ptoe** is a Python utility for splitting PDFs into images, driving local OCR via llama.cpp (llama-server), and converting OCR text to XHTML/EPUB. It provides a CLI (`mian.py`), PDF/fitz utilities (`pdfmanage.py`), llama-server orchestration (`llamamanage.py`), config CRUD with tkinter dialog helpers (`configmanage.py`), zh-CN/zh-TW string conversion (`stringmanage.py`), an optional browser-based manual-correction stage (`correctmanage.py`), and XHTML/EPUB output (`htmlmanage.py`, `epubmanage.py`). Packaged as flat Python scripts at repo root, not a Python package. **Git repo (single init commit — nearly all functionality lives in the uncommitted working tree; trust the source files, not `git log`/`git diff`, for current behavior).** Windows-oriented (llama-server paths, tkinter dialogs). Inline comments/docstrings are predominantly zh-CN; match that style when editing.

## Architecture & Data Flow

- **Entrypoint:** `mian.py` — argparse CLI. Top-level: `-e/--echo`, `--version`; **no args + interactive tty stdin (incl. double-clicking the packaged exe) → interactive terminal menu `_run_menu()`** (PDF→EPUB 转换 / 手动矫正 / 配置 / 模型管理 / 帮助 / 退出; helpers `_ask`/`_pause`, menu items `_menu_epub`/`_menu_correct`/`_menu_config`/`_menu_model`, item 5 帮助打印 CLI 用法); no args + non-interactive stdin prints `"<name> <version> — nothing to do"` and exits 0. Subcommands: `epub <pdf>` (full OCR→EPUB pipeline) and `correct [<pdf>]` (direct correction UI without OCR). Name/version read from `pyproject.toml` via tomllib (`_read_meta()`; fallback `ptoe`/`0.0.0`; **frozen-aware: reads `sys._MEIPASS/pyproject.toml` when PyInstaller-packaged**, `--add-data "pyproject.toml;."`). Exit codes: 0 success; epub exception or `epub_error` in result → stderr `Error: {e}` + exit 1.
- **PDF Pipeline:** `split_pdf_to_images` (`pdfmanage.py`; `dpi`/`fmt` are keyword-only) → `data/<pdf_stem>/` via PyMuPDF (`fitz`); page images 1-based (`1.png`, `2.png`…); **相同 PDF（sha256 一致）+ 相同 dpi/fmt 时复用已有分割图片** via `.ptoe_split.json` marker. `fitz` is lazily imported → `RuntimeError` if missing. Password-protected PDFs: tries empty-password auth, then raises. `createdic()` appends `_1`, `_2`… if dir exists (base = script dir, not cwd). `is_pdf_file()` searches for `%PDF-` anywhere in first 1 KB (not first-5-bytes).
- **Image queue:** `ImageItem` / `ImageQueue` (`pdfmanage.py`) — thread-safe FIFO. `ImageItem` base64 lazily materialized to temp file; `ImageQueue` supports `add/add_many/preload_all/get_next/peek/size/clear`.
- **OCR Pipeline:** `llamamanage.py` — module-global `_server_process`. `runserver()` starts llama-server.exe (config-driven) with GPU detection (`CUDA|Vulkan|ROCm|Metal` via `--list-devices`) and **`--log-verbosity 0`** (只输出 ERROR 级日志，屏蔽 find_slot/print_timing 刷屏；进度由 ptoe 侧打印). **host/port/temperature/repeat-penalty/parallel/cache-type-k/v/log-verbosity 等启动参数从 config.json 的 `llama_server_args` 读取**（缺失键跳过回退 llama-server 内置默认；可选 `n_gpu_layers` 键覆盖 GPU 自动检测，默认仍走 `--list-devices` 探测 + `--n-gpu-layers 999`）. **启动前 `_probe_server(model_name)` 探测 127.0.0.1:8080**（GET /health + /v1/models 比对模型 id）：已有同模型 → 复用 return True；模型不符 → 停掉本进程旧实例后重启，外部进程占用则 abort（避免旧实例存活时新 Popen 绑定失败、health 打到旧服务返回 ok 导致用错模型静默 OCR）。POSTs to OpenAI-compatible `/v1/chat/completions` via shared `requests.Session` (keep-alive). `_request_image_new` posts multimodal `content` blocks (text + image_url with base64 data URI) — **not** native `images:` field (see `ocr_compare.txt`). Payload includes **`max_tokens: MAX_TOKENS (=4096)`** and **`chat_template_kwargs: {"enable_thinking": thinking}`** — Qwen3 系模型默认隐藏思考链开启，OCR 时大部分 token 花在推理甚至死循环（实测单请求 4 万+ token、15s/页）；关思考后 100dpi 单页 ≈392 tokens、**~2-3s/页**（从 ~15s 提升）。非 Qwen 模板忽略该键，无副作用。**OCR prompt 由 config.json 的 `ocr_prompt` 管理**（mian.py 以 `get_config(show_dialogs=False).get("ocr_prompt") or OCR_PROMPT` 读取，缺失回退 llamamanage 的 `OCR_PROMPT` 常量；DEFAULT_CONFIG 含同值种子，外部配置缺该键时首次加载自动写入）。默认值即完整性提示词 `"请逐行完整识别图片中的全部文字，逐字输出，不得遗漏任何内容、不得省略、不得总结、不得翻译"`（0.8B 模型跳字靠它缓解，实测 +20% 字符）；**Prompts always get `"\n按原文原格式输出"` appended unless `thinking=True`**；`finish_reason=length`（触顶 max_tokens 截断）会打印 WARNING 提示检查该页。 `batch_infer` uses ThreadPoolExecutor; **config/model resolved once per batch** (avoids per-page `get_config()` lock contention + tkinter popups); **default max_workers=3** — visual models use ~4600-8700 image tokens/page; higher concurrency causes KV spill to CPU, slowing individual requests. Per-item failures captured in result dict (not raised); **配置解析失败时整批返回 error list，不再逐页兜底（S3）**. **每页请求后清理 ImageItem 临时文件/内存（P5）**. Server outlives the CLI; `stopserver()` to stop.
- **Configuration:** `configmanage.py` — `get_config()` auto-creates/repairs `config.json` (CWD-relative, thread-safe lock; **写入原子化 tempfile+os.replace，且仅在确有字段变更时写盘**（P1）；**tkinter 对话框在锁外弹出**（S2，避免对话框期间其他线程卡死在锁上）). Keys: `llama_server`, `models_dir`, `model_choices` (registry of `{name, mmproj}` objects: `HY`, `QWEN.8`, `QWEN2`, `QWEN4`, `PD`, `ULQ8`, `ULQ4`), `selected_model` (default `HY`), **`ocr_prompt`**（OCR 提示词，与 llamamanage.OCR_PROMPT 同值）、**`llama_server_args`**（嵌套 dict：host/port/temperature/repeat_penalty/parallel/cache_type_k/cache_type_v/log_verbosity）。**新增键经 `validate_and_patch_config` 递归 merge 自动补全并写盘**（外部配置缺参时程序自动写出默认值）。修改函数：`set_ocr_prompt(text)`（顶层键）、`set_llama_server_arg(name, value)`（嵌套键；`update_config` 只支持顶层）；`get_config(*, show_dialogs=False)` 跳过 tkinter 对话框（CLI/headless 读取用，校验+写盘仍执行）。CLI：`config show` 打印全部键（model_choices/ocr_prompt/llama_server_args 嵌套均含），`config set` 支持顶层四键 + 点分路径 `llama_server_args.<参数>`（经 set_llama_server_arg，含可选 n_gpu_layers）。tkinter dialogs pop when paths are missing/invalid (headless → `TclError` swallowed, no prompt).
- **String Conversion:** `stringmanage.py` — `ttos`/`stot` (zhconv wrappers) and `clean_and_structure_text` (strips `<think>`, collapses whitespace, splits paragraphs, optional 繁简; returns `{pages, body, paragraphs}`). Asserts cannot request both simplified and traditional at once. Per-page auto-processing inside `clean_and_structure_text` (before whitespace normalization): **`convert_bbox_text`** — PaddleOCR/ULQ4/ULQ8 输出格式 `label [x,y,x,y] text` 自动转换（`title`→`<h2>`、其他→`<p>`、`page_number`/`figure`/`image` 丢弃、非 bbox 行保留原样；内容 html-escape）; **`strip_page_numbers`** — 默认删除页首/页尾的独立页码行（`第N页`、`- 4 -`、`— 5 —`、`6 / 12`、纯数字），页面中间的数字与 4 位年份保留（保守策略）; **`detect_headings`**（2026-08）— 纯 OCR 文本标题启发式（在 strip_page_numbers 之后、normalize_spaces 之前逐页调用）：命中页整页转为 `<h1>`/`<p>`——候选行条件 = 去空白后 1≤len≤30、不以句末/引出行标点结尾（`。！？!?…，、；：`）、含 CJK/字母、非纯数字 → `<h1>`，其余行 `<p>`（html-escape）；含 `<` 的行（bbox 已转 HTML）原样跳过；**整页无命中原样返回（逐字节不变）**。产出 `<h1` 供 htmlmanage 按文章分页 + 标题进 EPUB 目录。
- **Manual Correction (optional, `--correct` only):** `correctmanage.py` — `correct_pages()` starts a local `ThreadingHTTPServer` with embedded HTML UI (left=image preview from PDF, right=contenteditable text one `<div>` per line). Blocks until browser closes > `idle_timeout` (default 600s) or 完成并转换. **Browser-close monitor:** heartbeat every 30s + `pagehide` beacon; auto-continues pipeline once browser gone > `idle_timeout` (avoids infinite hang). `preload_history=False` skips history cache — `epub --correct` passes this (fresh OCR wins over staged content); `correct <pdf>` keeps default (re-correct saved content). Versioned history cache at `data/correction_history/` (20 versions per file, preloaded on start). Markers (`全文`, `段落`, `注释标记`, `换页`) inserted as `<span class="ptoe-marker" data-ptoe-marker="...">` at caret; `apply_markers` converts them to `structured['articles']` for EPUB. `sanitize_html` whitelist: `<p>/<h1-6>/<strong>/<em>/<br/>/<span>` (with `data-ptoe-marker` + `class`), attributes stripped except `class="ptoe-note"`/`ptoe-align-*` 与标记 span 的 `ptoe-marker` 类（2026-08 起随载荷落盘；旧历史版本 serve 时由 `_ensure_marker_classes` 在 `/api/pages` 与 `/api/history/load` 补回——漏补则标记渲染为纯文本而非高亮，曾出 bug：历史载入路径漏补）； `<img>` 仅保留 src/alt/`ptoe-img-full`|`ptoe-img-fit` class（src 为空整体丢弃）。 Also supports: Markdown source mode, 繁简 `/api/convert`, rebindable keyboard shortcuts, 虚拟列表 for 1000+ pages. **性能/稳定性（2026-08 审查后）:** 预览图在服务生命周期内复用同一 `fitz.Document`（`preview_doc` + `preview_doc_lock`，渲染持锁；结束 close），JPEG 缓存为 **LRU 上限 200**（`OrderedDict` + `popitem(last=False)`）；历史列表 `_history_entries` 用文件签名（name+mtime_ns+size）轻量索引，文件未变不重复 json.loads；`state["pages"]` 跨线程读写经 `pages_lock` 快照（handler 与 correct_pages 返回值）；`_write_history_version`/`_overwrite_history` 返回 bool，失败时 `/api/save|stage|finish` 回 `ok:false` + error，前端 toast 红字（不再静默假装成功）；`_overwrite_history` 取最新版按 `mtime_ns` 而非文件名排序。**UI（U1-U6）:** 工具栏按功能分组（`.tb-group` 浅色圆角区块，格式按钮图标化），主操作组 `.tb-main` nowrap 右端常驻；toast 三色提示（成功/失败/警告）+ finish 按钮 loading 转圈；hintbar 可关闭（localStorage `ptoe_hint_hidden`）；滚动时显示「第 X / 共 Y 页」（`#pos`）；contenteditable/popup/图标按钮加 aria-label；虚拟列表前缀高度数组 `prefixH` + 二分替代 O(n) `prefixTop`。**新功能（2026-08）:** 文本智能清理（`clean_page_html` + 工具栏「清理」+ `/api/clean` 端点：段合并仅限相邻两个「普通段落」（无 class 的 `<p>` 且块内无行内标签——已设标题/格式/标记/图片的段落保留原结构不合并）且前块不以句末标点结尾（`。！？!?…` 及闭合括号/书名号 `）)】」』》〉`）、ASCII alnum 相接才补空格、段首 `#/*` 等符号清理、** 加粗符号清理（`_MD_BOLD_RE` 全 token 处理含句中）、汉字相邻半角标点→全角/字母数字间全角→半角、剥非白名单标签但 b/i 留给 sanitize 归一 strong/em）；图片插入（左栏「图」按钮→fetch 当前图→data URI 居中插入、`ptoe-img-full` 全画幅/`ptoe-img-fit` 局部、localStorage `ptoe_img_mode`、`htmlmanage.convert_document` 提取 data: 写 `OEBPS/Images/`）；搜索替换（工具栏「搜」按钮弹出模态框：正则开关、结果列表 ±40 字上下文+页码+`<mark>` 高亮、上限 200 条、点击结果跳转、↑↓ 上一个/下一个循环、「替换当前」仅替换选中那处（其余不动）/「全部替换」仅替换标签间文本 token，替换后自动重跑搜索刷新结果；搜索与替换用同一 token 切分保证序号一致）；原图模式（`correct <pdf>` 无分割图片或图片缺失时回退 PDF 高 DPI 渲染——`_FULL_DPI=220` + `_render_jpeg` 复用 `preview_doc`，保证原图始终可看；JS `img.onerror` 防循环：预览失败→尝试原图→原图失败显示「加载失败」）；虚拟列表滚动稳定（`#pages` 设 `overflow-anchor:none` 关浏览器原生锚定；**防滚动钳制回拉：`attach()` 在图片懒加载未就绪时不记录「仅文字」高度（heights[i] 留空走 est 估算），`reposition()` 把 host 高度下限设为 `scrollY+innerHeight+BUF*GAP`——可滚动高度绝不在滚动中收缩，否则浏览器会钳制 scrollY 造成「滚动中回滚」**；**滚动活动判定喂给 wheel/touchmove/scroll 三事件（`lastAnyScrollTs`，覆盖惯性滑动/滚动条拖拽/键盘翻页；`lastUserScrollTs` 仅 wheel/touchmove，供 withScrollStable 还原判断）**；`anchorScrollForHeightChange` 对完全位于视口上方的行高变化反向补偿 scrollY 且仅当无滚动活动时生效、`remeasure` 的 old 取 `heights[i] || est`；`withScrollStable` 包装 execCommand 操作防浏览器自动滚动跳页）；；延迟提示（localStorage `ptoe_tip_delay` 默认 600ms 可配置、提示含快捷键、全局 `#tip` 元素）；popup 两行 7+6 显示 + 「注」图标；虚拟列表高度修复（attach/remeasure 阈值 `h>0`/`h<=0 return` + `scheduleRemeasure` rAF 合并）。**撤回/前进（undo/redo）:** 工具栏「操作」组 ↶/↷ 按钮（ic-btn，初始 disabled）+ Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z 快捷键（capture 监听抢在「全局事件」快捷键处理器前，避免用户自定义绑定冲突）；**操作级快照**：连续输入（间隔 <800ms）合并为一步，格式/标记/对齐/搜索替换/清理/繁简转换/插入图片各算一步；输入起点用 `beforeinput`（含 IME/粘贴/拖放），`keydown`（排除 isComposing/229/ctrl/meta/alt，仅退格/删除/回车或单字符）/`compositionstart`/`paste` 兜底，`inDiscreteOp` 抑制离散操作期间误记；异步操作（clean/convert/insertImage/replace）在 fetch 前 `histBegin`、应用后 `histEnd`（只保留实际变化页，空则不入栈）；undo/redo 栈各保留**最近 10 步**（超出丢最旧，新操作清空重做栈），`histClear()` 在 md/富文本模式切换与历史版本载入时调用（快照源不一致）；恢复走 map+innerHTML+remeasure 路径（不触发历史、不抢滚动，anchor 在用户静止时自动补偿）；撤销/重做不重置 dirty/editedSet 标记（撤回≠回到已保存状态）。**UI 修复（2026-08）:** 选中文字弹出快捷菜单（`maybeShowPopup` 挂 document mouseup/keyup：跳过 isComposing/Ctrl/Meta/Alt、选区 !isCollapsed 且在 `.editable` 内才 `showPopup(range)`；`#popup` 补 fixed 定位 CSS（z-index:60）+ `.pop-btn` 基础样式；操作按钮点击置 `suppressPopupUntil` 250ms 防误弹；selectionchange 折叠即隐藏；滚动时 hidePopup）；工具栏格式组补齐 4 个 `data-op` 按钮——标题(标，p/div→h1→…→h6→p 循环)/正文(正)/清除格式(清)/注释(注)，现共 **13 个 data-op 按钮**（此前 heading/p/remove/note 只能走快捷键或已损坏的 popup）；`applyFontSize(v)` 补定义（设 CSS 变量 `--editor-font-size` + setStatus，此前 fontSizeSel 切换抛 ReferenceError 功能失效）。**多块统一格式化（2026-08）:** 选中跨多个块（段落/标题）时，格式操作对全部选中块统一生效——`applyToSelectedBlocks`（按选区起止块经 TreeWalker 收集中间所有 P/DIV/H1-6，逐块构造单块 range 调 fn，best-effort 容错，完成后恢复原选区）+ `_blocksBetween`；**7 类块格式化 op 全部支持多块**：bold/italic（execCommand + withScrollStable）、remove（removeFormat）、p/heading（`_convertBlockTag` 逐块换标签——p 不再用 execCommand formatBlock，Chrome 对跨块选区只转起始块）、note（classList.toggle ptoe-note）、align_*（三类 class 互斥）；marker_* 保持单点光标插入（语义如此）。边界修复：选区起点/终点为 .editable 直属文本节点时 startBlock===ed，`_blocksBetween` 从首块收集（此前恒空、多块操作失效）。**滚动/编辑回弹 + 性能（2026-08）:** 回弹/跳页根因——行高晚变（图片延迟加载/编辑/撤销）与补偿被抑制（img.onload 直接 remeasure、`fromImage` 跳过补偿、anchorScroll 400ms 滚动门控）→ 内容滑走/积压释放，滚动中突然跳页（滚到 180 页回跳 176、编辑 178 页跳到 177/179）。修复：img.onload → `scheduleRemeasure(i)`（单帧合并：批量 remeasure 后统一一次 `rebuildPrefix()`+`reposition()`）；**`remeasure`/`anchorScrollForHeightChange` 无门控、无 fromImage 跳过——任何行高变化（编辑/撤销/图片加载）与布局同帧原子补偿 scrollY，视口始终贴附，滚动/编辑不再跳页**；`deferLayout` 由调用方统一重建/重排；`updateViewport` 批量化——先收集 toAttach 全部 `attach(i,{defer:true})` 再统一 `measureRow(i)` 循环（一次布局），卸载循环后只重建/重排各一次（原先每行 O(n) 重建 + 强制 reflow）；`reposition` 改用 `parseFloat(row.style.top)` 不再读 offsetTop（免逐行 reflow）；新增 `measureRow(i)` 辅助，attach 默认路径行为不变（仅 updateViewport 用 defer）。**方向感知预挂载（内存换平滑）:** `PRELOAD=15` 滚动方向前方额外挂载 15 行（`_scrollDir`/`_viewportY` 由 updateViewport 动态跟踪），图片提前加载、行高提前稳定 → 滚到那里时已测量，滚动中几乎不再有补偿触发；代价为常驻 DOM/预览图内存略增（可接受）。**向上滚动空白/乱跳修复（2026-08）:** ① `anchorScrollForHeightChange` 判定改用行渲染位置（`row.getBoundingClientRect()`，`rect.bottom + 60 < 0` 才补偿）——旧版用估算前缀 `prefixTop(i) + oldH <= y` 判「视口上方」，未测量行按 est 顶替偏离真实布局（书内页高不均时偏差累积）→ 对视口内/下方行误补偿 → 向上滚 180→190 / 196→180 乱跳；② updateViewport 空滚区空白防护：scroll-lasso（reposition 的 host 高度下限 `scrollY+innerHeight+BUFFER*GAP`）允许 scrollY 超过内容总高时，二分 lo 钳到末尾、挂载行渲染顶远在视口上方 → 视口空白；判据 `scrollY > totalHeight() + innerHeight` 时回退 `_lastLo`（上一窗口下界）保留窗口——绝不彻底空白，滚回内容区即恢复；③ `measureRow` 无条件记录行高（含图片未就绪的无图高度，`h > 0` 即记录）——不再 est 顶替，窗口内行高始终真实，消除未测量行与实测行的坐标错位（向上滚动新挂载行与既有行重叠/间隙 = 视觉空白）；图片就绪后 onload → `scheduleRemeasure` 修正并即时补偿。**导出 TXT/DOCX（2026-08）:** 工具栏「导出」按钮弹出格式选择（导出为 DOCX / TXT），选格式后服务端弹 tkinter 保存对话框由用户选位置（headless 无对话框时回退 cwd 默认文件名 `history_name.pdf 去后缀` + 重名加 ` (n)` 序号）；`/api/export`（POST {format: txt|docx, pages:[{page,html}], path?}——body 带 `path` 时跳过对话框（测试用），用户取消返回 `{ok:false, cancelled:true}`，成功 `{ok:true, path, used_dialog}`）；纯 stdlib 实现（无 python-docx）：`_html_to_export_blocks(html)`（HTMLParser 子类，convert_charrefs=True，块类型 p/h1-6，`_SKIP_TAGS` 跳过 script/style 内容，`<br>`→`\n` 段内换行，空块丢弃）→ `_build_docx(blocks, path)`（zipfile 手工写 `[Content_Types].xml`/`_rels/.rels`/`word/document.xml`，XML 转义 &<>"，`\n` 替换为 `</w:t><w:br/><w:t xml:space="preserve">`，标题段 `<w:outlineLvl>` + rPr b+sz 半磅字号）；TXT 用 **utf-8-sig**（Windows 记事本 BOM）+ CRLF（write_text 平台默认）。前端：导出模态框 `#exportModalBg`（z-index:66，同 searchModalBg 模式）+ `exportFile(fmt)`（先 currentEditable+syncContent 兜底再 POST，cancelled→setStatus 提示，成功 toast+setStatus 显示路径）。**保存对话框由主线程弹出**（tkinter 线程安全：handler 把请求放入 `state["dlg_queue"]` 等待，`correct_pages` 主循环每轮 `_drain_dialog_queue` 弹框，`_pick_export_path` 加 `-topmost` 置顶防被浏览器遮挡；服务关闭时 `_abort_dialog_queue` 唤醒阻塞的 handler 返回 500，不挂起）。
- **Output:** `htmlmanage.py` — `HTMLConverter.convert_document` creates `OEBPS/` + `OEBPS/Images/`, writes CSS, cover, content (merged or per-page chapters), and `nav.xhtml` TOC. **The .epub is only packaged when `meta['package_epub']` is truthy** via `pack_from_oebps`. **merged 分支按 h1 换页（2026-08）:** 正文含 `<h1` 时按 `(?=<h1(?:\s|>))` 前瞻切分为多章（每篇一级标题文章 = 一个 `content_N.xhtml` = EPUB 新页开始），title 取首个 h1 内文（剥标签/unescape/压空白，空回退书名），首个 h1 前的前言块用书名；**`<h1` 来源 = stringmanage `detect_headings`（纯 OCR 自动识别）或手动矫正标记**；无 h1 路径逐字节不变（纯 OCR 无标题时仍单文件）。**EPUB 内相对路径一律正斜杠**（2026-08 修复）：Images/ 拼接用 `'Images/' + fname` 而非 `os.path.join`——Windows 下 join 产出反斜杠 `Images\img_1.png`，阅读器按 URI 解析失败导致图片/封面不显示（epubmanage ResourceMapper 的 `Images|Styles/` 正则只修斜杠、反斜杠漏网）。
- **EPUB:** `epubmanage.py` — `EPUBPacker` builds `META-INF/` + `OEBPS/Text|Styles|Images`, zip writes `mimetype` FIRST with `ZIP_STORED`. `toc.ncx` for EPUB2 only. Spine order: `cover.xhtml` → `nav.xhtml` → `content_*.xhtml` sorted by `_natural_key` (number-aware).

## Key Directories

- **Project Root:** All source and test files here (no src/ layout); also `config.json`, `pyproject.toml`, `uv.lock`, `USAGE.md`, `AGENTS.md`, `ocr_compare.txt`
- **data/** — Output dirs created by `createdic()` per PDF (e.g. `data/《毛泽东思想万岁》武汉钢二司编 第四卷/`). `1.png` 页图在相同输入下可跨运行复用（避免重复切图）；最终 .epub 也写入该目录 (e.g. `毛泽东思想万岁（1958—1960）.epub`, ~1.2 MB). `data/correction_history/<sha1>_<时间戳>_<随机>.json` holds versioned per-PDF corrected pages (one version per 保存/暂存/完成, capped 20/file, latest preloaded next run; managed via the 历史记录 dialog)
- **.venv/** — Local virtual environment (created by `uv`)
- **.opencode/**, **.crush/**, **.idea/**, **__pycache__/** — editor/tool state; ignore. `.opencode/memory/project.md` mirrors this file; keep them in sync
- **config.json** — Created/updated at runtime by `configmanage.py` (already present with the model registry)

## Development Commands

- **Sync deps:**
  ```sh
  uv sync
  ```
- **CLI:**
  ```sh
  uv run python mian.py [--version] [-e TEXT]
  uv run python mian.py epub <pdf> [--dpi 0] [--model HY] [--workers 3] [--timeout 600] [--title TITLE] [--author AUTHOR] [--lang zh-CN] [--out-dir DIR] [--epub-path PATH] [--thinking] [--correct] [--correct-timeout 600]
  uv run python mian.py correct [<pdf>] [--title TITLE] [--author AUTHOR] [--lang zh-CN] [--out-dir DIR] [--epub-path PATH] [--correct-timeout 600]
  uv run python mian.py config show                    # 查看当前配置（全部键：路径/模型/提示词/启动参数）
  uv run python mian.py config set llama_server <path> # 快捷修改 llama_server 路径
  uv run python mian.py config set ocr_prompt <text>   # 修改 OCR 提示词（config.json ocr_prompt）
  uv run python mian.py config set models_dir <path>   # 快捷修改 models_dir 路径
  uv run python mian.py config set llama_server_args.parallel 11  # 修改嵌套启动参数（点分路径，含可选 n_gpu_layers）
  uv run python mian.py model list|show|set|add|remove  # 模型注册表管理
  powershell -ExecutionPolicy Bypass -File .\pack.ps1   # PyInstaller 打包 dist\ptoe.exe（onefile+console，终端菜单）
  ```
- **Run tests:**
  ```sh
  uv run python -m unittest test_pdfmanage   # passes: 13 tests
  uv run python -m unittest test_mian        # passes: pipeline wiring (mocked OCR)
  uv run python -m unittest test_correctmanage  # BROKEN: does not import (missing convert_text_html; see Gotchas)
  uv run python -m unittest test_llamamanage  # passes: batch_infer config resolution + session reuse
  uv run python test_image_queue_request.py  # prints "test passed"; see Gotchas
  uv run python test_config_llama.py         # fails until `uv add --dev pytest` is run
  ```
- **Stop a leftover llama-server:**
  ```sh
  uv run python -c "from llamamanage import stopserver; stopserver()"
  ```

No build, lint, or type-check commands defined. No makefile or task runner present. No CI, pre-commit, or formatter config.

**Static checking:** `pyproject.toml` has a `[tool.pyrefly]` section (`project-includes` for `**/*.py*`, `**/*.ipynb`). Pyrefly runs in the editor LSP and reports **pre-existing** diagnostics — do not treat them as regressions from your changes:

- `llamamanage.py:46` — `models_dir may be uninitialized` (the `'models_dir' in locals()` guard in `check()` is a pyrefly-blind hack; `models_dir` is only bound inside the `if not llama:` branch)
- `configmanage.py:156/161` — values passed to `os.path.isfile`/`isdir` without narrowing (the dict-typed `models_dir` variant)
- `epubmanage.py` / `htmlmanage.py` — a handful of `LiteralString`/`None` assignability complaints (e.g. the `src` kwarg, result-dict keys `epub`/`epub_error`)
- `test_image_queue_request.py` — `json` param typed `None` in the fake `post`

## Code Conventions & Common Patterns

- **File layout:** Flat, all code at repo root, not installable as a package
- **Entrypoint:** Always use `mian.py` (deliberate typo, do NOT rename)
- **Exports:** `pdfmanage.py` uses `__all__` (`cpath`, `createdic`, `is_pdf_file`, `split_pdf_to_images`, `ImageItem`, `ImageQueue`) to mark its public API
- **PDF output:** All outputs go to `data/<name>/`
- **Config-driven paths:** `llamamanage.py` reads the llama-server.exe path and models dir from `config.json` (via `get_config()`); defaults point to `E:/xox/Tools/llama-c/` (Windows only)
- **Error handling:** Standard Python exceptions; error paths rarely tested. OCR functions return `{"result": None, "error": str(e)}` dicts instead of raising; `batch_infer` captures per-item failures rather than propagating
- **State:** No dependency injection — module-global `_server_process` in llamamanage.py; threading.Lock in configmanage.py
- **Concurrency:** threading (Lock, ThreadPoolExecutor), never asyncio
- **Comments:** Inline comments/docstrings are mostly zh-CN; match that when writing new code
- **CLI failure convention:** errors print `Error: {e}` to stderr, exit code 1

## Important Files

|File|Purpose|
|---|---|
|`mian.py`|CLI entrypoint: `-e/--echo`, `--version`, `epub <pdf>` + `correct <pdf>` subcommands; `pdf_to_epub()` pipeline, `correct_pdf()` direct correction UI, `_apply_correction()` shared post-correction structuring. Typo filename; do NOT rename|
|`pdfmanage.py`|PDF → image splitting + `ImageItem`/`ImageQueue`. Key: `cpath`, `createdic`, `is_pdf_file`, `split_pdf_to_images`. Exports via `__all__`|
|`llamamanage.py`|Manages llama-server.exe: `run`, `check`, `runserver`, `stopserver`, `request`, `request_image`, `batch_infer`|
|`configmanage.py`|Config read/write (`get_config`/`update_config`), tkinter dialogs, legacy aliases|
|`stringmanage.py`|zhconv wrapper — `ttos`, `stot`, `clean_and_structure_text`|
|`correctmanage.py`|可选手动矫正：本地 HTTP 服务 + 内嵌 HTML 界面（左图右文、虚拟列表、标记/标题/对齐/注释、繁简、Markdown 模式、历史缓存）。`correct_pages`/`sanitize_html`/`apply_markers`/`initial_html`/`clean_page_html`。仅 `--correct`/`correct` 命令启用|
|`htmlmanage.py`|structured text → XHTML/OEBPS (HTMLConverter, CSSManager, HTMLValidator)；`_render_fragment` 白名单标记渲染 + 标题锚点 + 标题目录收集；`structured['articles']` → 每篇一个内容页；`ptoe-img-*` class + `convert_document` 提取 data: 图片写 `Images/`|
|`epubmanage.py`|OEBPS → .epub (EPUBPacker, EPUBMetadata, ResourceMapper, `pack_from_oebps`)；`pack_from_oebps` 接受 htmlmanage 的标题目录（href 可带 #锚点，映射时保留 fragment）；EPUB2 `toc.ncx` 用同一目录|
|`USAGE.md`|zh-CN user tutorial for the epub pipeline (commands, flags, server lifecycle)|
|`ocr_compare.txt`|Records the OCR payload-format fix (see Gotchas)|
|`test_pdfmanage.py`|unittest suite covering all pdfmanage exports (13 tests, passing)|
|`test_mian.py`|unittest for the wired `pdf_to_epub` pipeline (mocked OCR) + no-args terminal-menu dispatch (tty → menu, pipe → "nothing to do")|
|`test_correctmanage.py`|unittest for correctmanage (sanitize_html incl. img 白名单, clean_page_html, markup rendering, `--correct` wiring, browser-close monitor, /api/clean 与 /api/export 端点). **Currently does not import** — missing `convert_text_html` (see Gotchas)|
|`test_llamamanage.py`|unittest for llamamanage (batch_infer config resolution + session reuse + single-failure handling)|
|`test_stringmanage.py`|unittest for stringmanage (bbox conversion, page-number stripping, clean_and_structure integration)|
|`test_image_queue_request.py`|Script-style check (plain asserts, monkeypatches `requests.post`)|
|`test_config_llama.py`|pytest-style; requires pytest (NOT in venv)|
|`pyproject.toml`|Metadata (name `ptoe`/version `0.1.0`), deps, Python ≥3.11, `[tool.pyrefly]`|
|`uv.lock`|Dependency lockfile (generated by uv; locks pymupdf 1.28.0, requests 2.34.2, zhconv 1.4.3)|
|`config.json`|Runtime config — auto-created/repaired by `configmanage.py`|
|`pack.ps1`|PyInstaller 打包脚本：`dist\ptoe.exe`（onefile+console，`--noupx`，`--add-data "pyproject.toml;."` + `--collect-all pymupdf`；`uv run --with pyinstaller`，不改 pyproject/uv.lock）|

## Runtime/Tooling Preferences

- **Python version:** ≥3.11 (pyproject.toml)
- **Package manager:** `uv` (`pyproject.toml`, `uv.lock`)
- **Dependencies:** pymupdf, requests, zhconv — nothing else (pytest NOT included)
- **Platform:** llama-server paths are Windows-specific; tkinter dialogs on Windows desktop
- **No linter/formatter/typechecker**: Write idiomatic Python only — no CI will check. Pyrefly via editor LSP only

## Testing & QA

- **Framework:** stdlib `unittest` only; pytest is not installed. `test_image_queue_request.py` is a plain script, `test_config_llama.py` is pytest-style
- **test_pdfmanage.py** (4 classes / 13 tests, passing): covers `createdic`, `is_pdf_file`, `cpath`, `split_pdf_to_images`. Uses `tempfile.mkdtemp` & `shutil.rmtree`; honors `TEST_PDF_PATH` env var, falls back to a hardcoded `E:\MYBooks\books\毛泽东思想\主席与毛远新同志谈话纪要.pdf` if it exists, then to a generated 3-page fitz temp PDF named `test_split`; skips if fitz is missing; scrubs leftover `data/<name>` dirs in setUp
- **test_mian.py** (classes `TestPdfToEpub` + menu dispatch, 5 tests, passing): generates a temp PDF, monkeypatches `llamamanage.batch_infer` and `mian._ensure_server` (mian lazy-imports batch_infer), asserts a valid EPUB — mimetype first + `ZIP_STORED`, `META-INF/container.xml`, `OEBPS/content.opf`, single merged `OEBPS/Text/content_1.xhtml` with page order preserved, spine idrefs present in manifest, manifest hrefs exist in the zip; plus a `_natural_key` sort test; plus no-args dispatch (tty-faked stdin → menu, piped stdin → "nothing to do", EOF/empty input → menu exits with 已退出). tearDown scrubs `data/sample*`
- **test_correctmanage.py** (**BROKEN — does not import**): `from correctmanage import (...)` fails with `ImportError: cannot import name 'convert_text_html'` — the function is referenced by the `/api/convert` handler (correctmanage.py:1681) but has no definition or import anywhere (working tree mid-refactor; see Gotchas). Once the import is fixed, the suite covers: `sanitize_html` whitelist (incl. marker span + `ptoe-note` class + `ptoe-align-*` alignment classes), `initial_html` line→`<div>` preservation, `apply_markers` (join merge across pages / 段首·段尾·段中 join / chapter `<h2>` / full article split / marker-only blocks / 注释替换·合并·括号归一·数量不匹配抛错 / alignment class 保留), `_render_fragment` markup + marker-strip + heading anchors + align classes, `convert_document` with `articles` → one content file per article, `convert_text_html`（繁简只转文本节点）+ `/api/convert` 端点（无状态、非法 mode 400）、`clean_page_html`（段合并/句末不合并/段首符号/中英文标点/标签剥除/幂等）+ `/api/clean` 端点（无状态）、`_html_to_export_blocks`（跳 script/style、br→换行、实体还原）+ `/api/export` 端点（body 带 path 跳过对话框：txt utf-8-sig 内容/BOM、docx zip 含 word/document.xml + 标题 outlineLvl + w:br + & 转义、非法 format 400、对话框取消 cancelled、headless 回退路径）、sanitize img 白名单（src/alt/`ptoe-img-*` class 保留、evil 属性剥、src 空丢弃）、`_history_pages_for_init`（preload_history 开关）、`pdf_to_epub(correct=True)` wiring, browser-close monitor (`_browser_gone` + heartbeat/gone endpoints + repeat-finish `on_convert` + 暂存写历史缓存 + 保存覆盖当前版本（`_overwrite_history`）+ 历史列表多版本/选中删除/全部删除). Run together: `uv run python -m unittest test_pdfmanage test_mian test_correctmanage test_llamamanage test_stringmanage` → **41 tests pass + 1 import error** (test_correctmanage) — currently NOT green; define `convert_text_html` first
- **test_llamamanage.py** (unittest, passing): covers `batch_infer` config resolution (model resolved once per batch, not per-page), `requests.Session` reuse across pages, and single-failure-doesn't-abort-batch behavior. Run: `uv run python -m unittest test_llamamanage`
- **test_stringmanage.py** (20 tests, passing): bbox conversion (title→`<h2>`, text→`<p>`, page_number/figure/image dropped, non-bbox lines kept, html-escape), page-number stripping (首/尾行 `第N页`/`- 4 -`/`— 5 —`/`6 / 12`/纯数字，中间与 4 位年份保留), clean_and_structure integration. Run: `uv run python -m unittest test_stringmanage`
- **test_image_queue_request.py**: script-style; fakes `requests.post` and asserts the OpenAI vision payload shape (`content[1]['image_url']['url'] == 'data:image/png;base64,...'`); run via `uv run python test_image_queue_request.py`; prints `test passed`
- **test_config_llama.py**: pytest-style (2 module-level test functions); fails with `ModuleNotFoundError: pytest` until `uv add --dev pytest`; also triggers `get_config()` (tkinter dialogs)
- **Coverage:** Core pdfmanage exports + wired pipeline covered; error paths and OCR request handling not unit-tested

## Gotchas

- **`request_image` is defined TWICE in `llamamanage.py`**: the old-style `def request_image` at line 331 is dead code (hardcodes `timeout=60`, `stop: ["\n\n"]`, uses `_reload_config()`, no `max_tokens`/`enable_thinking`), shadowed at the bottom (line 598) by `request_image = _request_image_new` (defined at line 499), a duck-typed version accepting any object with a callable `get_base64()` and threading `timeout` through. Edit the *new* function; do not delete the rebinding. `batch_infer` calls `_request_image_new` directly.
- **`correctmanage.py` references an undefined `convert_text_html`** at line 1681 (the `/api/convert` handler): no definition or import exists anywhere, so `test_correctmanage.py` currently fails to import (`ImportError: cannot import name 'convert_text_html' from 'correctmanage'`). The file is mid-refactor — any work touching 繁简 conversion in the correction UI must define this function (or remove the reference).
- **The embedded UI script is fragile**: `_UI_HTML` in correctmanage.py contains a ~1700-line inline `<script>` inside a Python raw string. It was once broken by deleted function headers/closers (silent until `node --check`). After ANY edit to that script, extract it and syntax-check: PowerShell `$l = Get-Content correctmanage.py; $s=($l|Select-String '^\s*<script>').LineNumber; $e=($l|Select-String '^\s*</script>').LineNumber; ($l[$s..($e-2)]) -join "`n" | Set-Content "$env:TEMP\ui.js" -Encoding utf8; node --check "$env:TEMP\ui.js"`. Also never introduce a `"""` sequence inside the raw string. **HTML 结构同理会中招**：`.tb-group` 组必须包在 `<div id="toolbar">` 内（CSS 的 `#toolbar{position:sticky...}` 与 JS 的 `#toolbar button[data-op]` 绑定都依赖它）——包裹层曾丢失导致工具栏不悬浮、按钮点击全部失效（2026-08 修复；现为 sticky 悬浮、最多两行紧凑布局，`.tb-label` 已移除，靠 title/aria-label 区分组义）。**函数定义同样会中招**：重构还可能删掉顶层绑定引用的函数定义——首个未定义的 handler 在加载期抛 `ReferenceError` 中断整个脚本，其后所有绑定（含工具栏 `data-op` 点击）全部失效（2026-08 修复：`openSearchModal`/`closeSearchModal`/`openExportModal`/`closeExportModal` 曾缺失）。`node --check` 查不出未定义名，编辑后除语法检查外还要核对每个顶层 `addEventListener(..., NAME)` 的目标函数已定义（可用 `%TEMP%\ptoe_ui_harness2.mjs` 或 grep）。富文本注释标记经 `insertMarkerAtCaret` 插在光标处（`range.collapse(false)` + `insertNode` 自动切分文本节点），服务端 `apply_markers` 保持标记位置，不再段首插入。**滚动驱动虚拟列表曾丢失**：滚动稳定重构后 scroll/wheel/touchmove 监听只写 `lastAnyScrollTs`/`lastUserScrollTs` 时间戳、从不调用 `updateViewport()`，导致虚拟列表只挂载初始窗口 `[0, BUFFER+viewRows)`（360 页书只出 18 行），滚动不挂载新行、后续页全空白（2026-08 修复：`scroll` 监听改为 `() => { markAnyScroll(); scheduleViewport(); }`，`scheduleViewport()` 以 `requestAnimationFrame` 节流调用 `updateViewport()`；滚动稳定性仍由时间戳守卫（`lastAnyScrollTs`/`lastUserScrollTs` 现仅服务 withScrollStable 还原判断；`anchorScrollForHeightChange` 已无门控——任何行高变化即时同帧补偿 scrollY）。另 `loadHistoryVersion` 在 `heights` 重置后补 `rebuildPrefix()`（prefixH 旧累计值不可跨会话复用，重建按 est 估算）。验证：`%TEMP%\opencode\ptoe_ui_harness2.mjs` 内含滚动模拟断言（v5：360 页 init 后 33 行（PRELOAD）→ 模拟 scrollY=50000 后 maxIdx>100 + 行渲染位置 rect 覆盖下的视口上方行高变化即时补偿（51080）断言 + 空滚区空白守卫断言（scrollY=10_000_000 超出估算总高 → 保留上一窗口不彻底空白、不新增错误行））+ 多块统一格式化断言（3 块选区 heading→全 H1 / note→ptoe-note / align_center / p→全 P）。
- **`ocr_compare.txt`** at repo root records the payload-format fix: the legacy `images:`-field request echoed back the prompt (garbage OCR), while the current `image_url` content-block variant works. Do not revert the payload format.
- **`config.json` diverges from `DEFAULT_CONFIG` on this machine**: `models_dir` is `E:/model` and `model_choices` names carry relative subdir prefixes (e.g. `huiyuan/HunyuanOCR.BF16.gguf`, `qwen3.5/...`, `paddleocr/...`, `Unlimited/...`); `check()`/`runserver()` join them via `os.path.join(models_dir, name)`.
- **`get_config()` pops interactive tkinter file/dir chooser dialogs** when `llama_server`/`models_dir` are missing or point at non-existent paths, and blocks until dismissed (headless environments swallow the TclError and skip). Every `llamamanage` call path (including `test_image_queue_request.py`) triggers `get_config()`, so on a desktop the script **hangs on the dialog** if the `E:/xox/...` paths don't exist — point `config.json` at valid paths first, or run headless.
- `llamamanage.py:46` — `check()` relies on a `'models_dir' in locals()` guard; if `llama` is passed in, `models_dir` is never bound and the fallback `os.path.dirname(llama)` is used.
- `createdic()` in `pdfmanage.py` uses incremental suffix (`_1`, `_2`) if the target dir exists; base dir is the script dir, not cwd
- OCR prompts always append `"按原文原格式输出"` unless `thinking=True`
- `stringmanage.clean_and_structure_text` asserts you can't request both simplified and traditional conversion at once
- The llama-server started by `runserver()` **outlives the CLI** — call `stopserver()` to stop it
- **`correctmanage.correct_pages` blocks the pipeline** until the user clicks 完成并转换 in the browser (or Ctrl+C). It starts a local server on 127.0.0.1 (ephemeral port) and opens the default browser; only `--correct` enters this stage. **Since the browser-close monitor, closing the tab/window no longer hangs the CLI**: the page heartbeats every 30s and sends a `pagehide` beacon; once gone > `idle_timeout` (default 600s, CLI `--correct-timeout`) the pipeline auto-continues with the last saved content. Preview images are rendered from the PDF (not the full-res PNGs) to avoid loading lag; **the same `fitz.Document` is reused for the whole server lifetime and closed on shutdown (P2)**; if the PDF path is unavailable they fall back to the original page images.
- **History preload vs fresh OCR**: `correct_pages(preload_history=False)` skips loading the latest history cache — the `epub --correct` pipeline passes this so a re-recognized book shows the NEW OCR text, not the last staged/saved content (use `correct <pdf>` to re-correct saved content, which still preloads).
- `USAGE.md`'s `model_choices` example (flat string names) is stale vs the real `{name, mmproj}` objects
- Root `.gitignore` ignores `__pycache__/`, `*.py[cod]`, and PyInstaller build artifacts (`build/`, `dist/`, `*.spec` — all regenerated by `pack.ps1`); `.idea/`, `.venv/`, `.opencode/`, `.crush/` carry their own (`.crush/.gitignore` is just `*`)

---
